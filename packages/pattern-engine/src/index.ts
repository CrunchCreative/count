// Deterministic stats engine — hit rates, consistency, volatility, streaks, confidence.
// Pure TypeScript. No React Native, no network, no I/O. Unit-testable in isolation.
// Skeleton only — populated in a later phase.
export {};
