# Hoist NativeWind's transitive deps so Metro (which runs with hierarchical
# lookup disabled in this monorepo) can resolve them from the app's node_modules.
# Scope narrowly — broad hoist patterns reintroduce phantom-dep risk.
public-hoist-pattern[]=*nativewind*
public-hoist-pattern[]=*react-native-css-interop*
